encoder:
  type: 'resnet34'
method:
  type: 'supervised'
  nb_classes: 5994
trainer:
  epochs: 200
evaluation:
  validation:
    - type: 'sv_cosine'
  test:
    - type: 'sv_cosine'
dataset:
  train: 'voxceleb2_train.csv'
  augmentation:
    enable: true
