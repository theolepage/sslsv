encoder:
  type: 'resnet34'
method:
  type: 'simclr'
trainer:
  epochs: 3
dataset:
  ssl: true
  max_samples: 1024
evaluation:
  num_frames: 10
  frame_length: 56240
  validation:
    - type: 'sv_cosine'
  test:
    - type: 'sv_cosine'
